{
	"name": "f2-repro-app-fixture",
	"private": true,
	"type": "module",
	"description": "Integration-test fixture for F2 reproduction. The @harperfast/oauth dep is installed at test setup time via scripts/install-fixtures.js (npm pack + install)."
}
